import 'package:flutter/material.dart';

class EditTasks extends StatelessWidget {
  const EditTasks({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Edit Taaks ')));
  }
}
